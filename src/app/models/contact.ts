export class Contact {
    name: string;
    email: string
    phoneNumber: number
    subject: string;
    message: string;
}

export interface $Contact {
    id: string;
    name: string;
    email: string
    phoneNumber: number
    subject: string;
    message: string;
}